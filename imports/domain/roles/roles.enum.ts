export enum Role {
  Admin = 'admin',
  Member = 'member',
  Staff = 'staff',
}

export enum Scope {
  Members = 'members',
  Users = 'users',
}

export enum Permission {
  Delete = 'delete',
  Read = 'read',
  Write = 'write',
}
